# DOS Technique Samples

This is the repository's searchable knowledge base. Every reusable successful
example has its own directory, a small Makefile, and a `README.md` starting
with TOML Markdown frontmatter. See `AGENTS.md` for the required metadata and
promotion rules.
