# Hello

The smallest current Free Pascal DOS floppy. It prints `Hello from DOS!` and
is useful as a smoke test for the i8086/MS-DOS Free Pascal toolchain.

Build with `make build`, execute the smoke test with `make test`, or create a
360 KiB FAT12 disk with `make image`.
