# Type Fury

A five-round public-domain typing game written in Open Watcom C for 16-bit
real-mode DOS. The focused input-handling test runs through the same Open
Watcom and NTVDM containers as the game.

Use `make build`, `make test`, or `make image`. The full keyboard-driven game
should be checked through DOSBox Automation when its screen behavior changes.
