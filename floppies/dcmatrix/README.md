# DeciMatrix Floppy

Packages DeciMatrix 1.0, an 8086-compatible Matrix-style screen saver by
Bruno Felix Rezende Ribeiro. It is GPLv3 software and ships with `COPYING`.

The program changes the video mode and does not restore it on exit; run
`MODE CO80` after quitting if necessary. Build with `make image`.
